sap.ui.define([
    "sap/ui/core/mvc/Controller"
], function (Controller) {
    "use strict";

    // Controlador raiz: apenas hospeda o contêiner de navegação (App).
    return Controller.extend("sapuccino.controller.App", {});
});
